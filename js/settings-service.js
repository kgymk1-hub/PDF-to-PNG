export const DEFAULTS={mode:'x',x:{width:'1600',trim:'on'},normal:{format:'png',scale:'2',range:'',background:'white',trim:'off',quality:80},draft:{title:'',date:'',note:'詳細は添付画像をご確認ください。',tags:'',altTemplate:'PDF資料の{page}ページ目を画像化したものです。タイトル、表、注記が含まれています。'}};
const KEY='postpng-maker-settings-v2';
export function loadSettings(){try{return structuredClone({...DEFAULTS,...JSON.parse(localStorage.getItem(KEY)||'{}')});}catch{return structuredClone(DEFAULTS)}}
export function saveSettings(s){localStorage.setItem(KEY,JSON.stringify({mode:s.mode,x:s.x,normal:s.normal,draft:s.draft}));}
export function parsePageRange(input,total){const raw=(input||'').trim(); if(!raw||raw==='全ページ') return Array.from({length:total},(_,i)=>i+1); const out=[]; for(const part of raw.split(',')){const p=part.trim(); if(!p) continue; if(p.includes('-')){const [a,b]=p.split('-').map(v=>Number(v.trim())); if(!Number.isInteger(a)||!Number.isInteger(b)||a<1||b<a) throw new Error('ページ範囲の指定が正しくありません。例: 1-4,8'); for(let n=a;n<=b;n++) out.push(n);}else{const n=Number(p); if(!Number.isInteger(n)||n<1) throw new Error('ページ番号は1以上の数字で指定してください。'); out.push(n);}} const uniq=[...new Set(out)]; if(uniq.some(n=>n>total)) throw new Error(`ページ範囲がPDFのページ数（${total}ページ）を超えています。`); return uniq;}
export const safeBaseName=(name='converted_pdf')=>name.replace(/\.pdf$/i,'').replace(/[^a-z0-9_-]+/gi,'_').replace(/^_+|_+$/g,'')||'converted_pdf';
export const formatBytes=(bytes)=> bytes>=1048576?`${(bytes/1048576).toFixed(1)}MB`:`${Math.ceil(bytes/1024)}KB`;

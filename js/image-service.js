export async function canvasToBlob(canvas,format='png',quality=.8){return new Promise((res,rej)=>canvas.toBlob(b=>b?res(b):rej(new Error('画像Blobの生成に失敗しました。')),format==='jpeg'?'image/jpeg':'image/png',quality));}
export function resizeCanvas(src,targetWidth){if(targetWidth==='original'||!targetWidth) return src; const width=Number(targetWidth); if(!width||src.width<=width) return src; const height=Math.round(src.height*(width/src.width)); return copyCanvas(src,width,height,false);}
export function copyCanvas(src,width=src.width,height=src.height,alpha=false,bg='#fff'){const c=document.createElement('canvas'); c.width=width; c.height=height; const ctx=c.getContext('2d',{alpha}); if(!alpha){ctx.fillStyle=bg; ctx.fillRect(0,0,width,height);} ctx.imageSmoothingEnabled=true; ctx.imageSmoothingQuality='high'; ctx.drawImage(src,0,0,width,height); return c;}
export function trimWhitespace(src,{threshold=240,padding=16,background='#fff'}={}){const ctx=src.getContext('2d'); const {width:w,height:h}=src; if(!w||!h) return src; const data=ctx.getImageData(0,0,w,h).data; let minX=w,minY=h,maxX=-1,maxY=-1; const white=(i)=>data[i]>=threshold&&data[i+1]>=threshold&&data[i+2]>=threshold&&(data[i+3]??255)>0; for(let y=0;y<h;y+=2){for(let x=0;x<w;x+=2){const i=(y*w+x)*4; if(!white(i)){if(x<minX)minX=x;if(y<minY)minY=y;if(x>maxX)maxX=x;if(y>maxY)maxY=y;}}} if(maxX<0) return src; minX=Math.max(0,minX-padding); minY=Math.max(0,minY-padding); maxX=Math.min(w-1,maxX+padding); maxY=Math.min(h-1,maxY+padding); const nw=maxX-minX+1, nh=maxY-minY+1; if(nw>w*.94&&nh>h*.94) return src; const c=document.createElement('canvas'); c.width=nw; c.height=nh; const out=c.getContext('2d',{alpha:false}); out.fillStyle=background; out.fillRect(0,0,nw,nh); out.drawImage(src,minX,minY,nw,nh,0,0,nw,nh); return c;}

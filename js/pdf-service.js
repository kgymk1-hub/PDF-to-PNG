import {canvasToBlob,resizeCanvas,trimWhitespace} from './image-service.js';
let pdfjsPromise;
async function getPdfjs(){if(!pdfjsPromise){pdfjsPromise=import('../libs/pdf.min.js').catch(()=>import('https://cdn.jsdelivr.net/npm/pdfjs-dist@4.10.38/build/pdf.min.mjs'));} const pdfjs=await pdfjsPromise; pdfjs.GlobalWorkerOptions.workerSrc='https://cdn.jsdelivr.net/npm/pdfjs-dist@4.10.38/build/pdf.worker.min.mjs'; return pdfjs;}
export async function loadPdf(file){const pdfjs=await getPdfjs(); try{return await pdfjs.getDocument({data:await file.arrayBuffer()}).promise;}catch(e){if(e?.name==='PasswordException') throw new Error('パスワード付きPDFには現在対応していません。'); throw new Error('PDFの読み込みに失敗しました。ファイルが破損しているか、対応していない形式の可能性があります。');}}
export async function renderPage(pdf,pageNumber,opts){const page=await pdf.getPage(pageNumber); const viewport=page.getViewport({scale:opts.scale}); const canvas=document.createElement('canvas'); canvas.width=Math.ceil(viewport.width); canvas.height=Math.ceil(viewport.height); const alpha=opts.background==='transparent'&&opts.format==='png'; const ctx=canvas.getContext('2d',{alpha}); if(!alpha){ctx.fillStyle=opts.background==='black'?'#000':'#fff'; ctx.fillRect(0,0,canvas.width,canvas.height);} await page.render({canvasContext:ctx,viewport,background:alpha?undefined:(opts.background==='black'?'#000':'#fff')}).promise; let output=opts.trim?trimWhitespace(canvas,{background:opts.background==='black'?'#000':'#fff'}):canvas; output=resizeCanvas(output,opts.width); const blob=await canvasToBlob(output,opts.format,opts.quality/100); return {blob,width:output.width,height:output.height};}
